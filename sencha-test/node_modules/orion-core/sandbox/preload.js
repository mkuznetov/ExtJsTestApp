window.ST = window.ST || { };

var path = require('path');

ST.serveDir = path.normalize(__dirname + '/../serve/');
ST.webdriverio = require('webdriverio');
/**
 * Property indicating whether the current test is running
 * "in the sandbox" or not. WebDriver scenarios are run
 * "in the sandbox".
 */
ST.isSandbox = true;

ST.nativewindow = require('native-window');
